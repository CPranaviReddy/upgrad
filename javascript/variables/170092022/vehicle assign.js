class Point3D extends Point2D {
  constructor (x, y, z) {
      super();
      this.move(x, y, z);
  }
  move (x, y, z) {
      super.move(x, y);
      this.z = z;
  }
}




class Vehicle{
  constructor (name,capacity){
    this.name=name;
    this.capacity=capacity;
  }
  class  2-W,3-W extends Vehicle{
    constructor (#name,#capacity) {
      super();
      this.move(#name,#capacity);
  }
  move (#name,#capacity) {
      super.move(#name,#capacity);
      this.#name = #name;
      this.#capacity = #capacity;
  }

  }
  static display
    #capacity = 4000W;    //private variable
     const vehicle=new vehicle(name,capacity)  
  
(name, capacity) {
    this.name = name;
    this.capacity = capacity;
    startEngine
    stopEngine
    run
    brake
  }
  start engine()
  $(this.name)


}